{
  "compilerOptions": {
    "module": "Esnext", // Or "ESNext" if you use import/export
    "moduleResolution": "Bundler", // "Bundler" is designed to mimic the resolution of modern tools like Vite/Webpack
    "target": "ES2020",
    "jsx": "react", // Add this if you're using React/JSX
    "baseUrl": ".", // Tells VS Code that imports can be relative to the root
    "paths": {
      "@/*": ["src/*"] // Example: allows you to do 'import Button from "@/components/Button"'
    }
  },
  "include": ["src"] // Or whichever folders contain your source code
}
